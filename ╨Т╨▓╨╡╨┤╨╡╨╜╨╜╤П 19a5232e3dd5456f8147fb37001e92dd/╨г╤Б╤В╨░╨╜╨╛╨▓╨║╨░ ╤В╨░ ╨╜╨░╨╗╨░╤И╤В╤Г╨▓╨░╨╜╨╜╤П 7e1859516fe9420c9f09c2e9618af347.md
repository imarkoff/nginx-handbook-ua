# Установка та налаштування

## ⬇ Встановлення готового пакунка

Встановлення NGINX з відкритим вихідним кодом з пакунка набагато простіше і швидше, ніж збірка з коду, але збірка з коду дозволяє вам компілювати нестандартні модулі. Готові пакунки доступні для більшості популярних дистрибутивів Linux.

### Debian, Ubuntu

1. Оновіть пакети:
    
    ```bash
    sudo apt update && sudo apt upgrade
    ```
    
2. Встановіть NGINX:
    
    ```bash
    sudo apt install nginx
    ```
    

### **RHEL, CentOS, Oracle Linux, AlmaLinux, Rocky Linux**

1. Встановіть EPEL:
    
    ```bash
    sudo yum install epel-release
    ```
    
2. Оновіть індекси пакетів системи:
    
    ```bash
    sudo yum update
    ```
    
3. Встановіть nginx:
    
    ```bash
    sudo yum install nginx
    ```
    

### Arch Linux

1. Оновіть бази даних пакетів:
    
    ```bash
    sudo pacman -Syy
    ```
    
2. Встановіть nginx:
    
    ```bash
    sudo pacman -S nginx
    ```
    

### SUSE Linux

1. Встановіть prerequisites:
    
    ```bash
    sudo zypper install curl ca-certificates gpg2
    ```
    
2. Налаштуйте репозиторій `zypper` для стабільних пакунків nginx:
    
    ```bash
    sudo zypper addrepo --gpgcheck --type yum --refresh --check \
    '[http://nginx.org/packages/sles/$releasever_major](http://nginx.org/packages/sles/$releasever_major)' nginx-stable
    ```
    
3. Якщо хочете використовувати основні пакунки nginx, виконайте замість цього наступну команду:
    
    ```bash
    sudo zypper addrepo --gpgcheck --type yum --refresh --check \
    '[http://nginx.org/packages/mainline/sles/$releasever_major](http://nginx.org/packages/mainline/sles/$releasever_major)' nginx-mainline
    ```
    
4. Імпортуйте офіційний ключ підпису nginx, щоб `zypper/rpm` міг перевірити справжність пакунків. Отримайте ключ:
    
    ```bash
    curl -o /tmp/nginx_signing.key [https://nginx.org/keys/nginx_signing.key](https://nginx.org/keys/nginx_signing.key)
    ```
    
5. Переконайтеся, що завантажений файл містить правильний ключ:
    
    ```bash
    gpg --with-fingerprint /tmp/nginx_signing.key
    ```
    
    На виході має бути повний відбиток `573B FD6B 3D8F BC64 1079 A6AB ABF5 BD82 7BD9 BF62`, як показано нижче:
    
    ```bash
    pub 2048R/7BD9BF62 2011-08-19 [expires: 2024-06-14].
    	Key fingerprint = 573B FD6B 3D8F BC64 1079 A6AB ABF5 BD82 7BD9 BF62
    uid nginx signing key <[signing-key@nginx.com](mailto:signing-key@nginx.com)>
    ```
    
6. Імпортуйте ключ до бази даних rpm:
    
    ```bash
    sudo rpmkeys --import /tmp/nginx_signing.key
    ```
    
7. Для установки nginx виконайте команду:
    
    ```bash
    sudo zypper install nginx
    ```
    

### Alpine Linux

1. Встановіть prerequisites:
    
    ```bash
    sudo apk add openssl curl ca-certificates
    ```
    
2. Налаштуйте репозиторій apk для стабільних пакунків nginx:
    
    ```bash
    printf "%s%s%s\n" \
    "http://nginx.org/packages/alpine/v" \
    `egrep -o '^[0-9]+\.[0-9]+' /etc/alpine-release` \
    "/main" \
    | sudo tee -a /etc/apk/repositories
    ```
    
    Для основних пакунків nginx:
    
    ```bash
    printf "%s%s%s\n" \
    "http://nginx.org/packages/mainline/alpine/v" \
    `egrep -o '^[0-9]+\.[0-9]+' /etc/alpine-release` \
    "/main" \
    | sudo tee -a /etc/apk/repositories
    ```
    
3. Імпортуйте офіційний ключ підпису nginx, щоб apk міг перевірити автентичність пакунків. Отримайте ключ:
    
    ```bash
    curl -o /tmp/nginx_signing.rsa.pub https://nginx.org/keys/nginx_signing.rsa.pub
    ```
    
4. Переконайтеся, що завантажений файл містить правильний ключ:
    
    ```bash
    openssl rsa -pubin -in /tmp/nginx_signing.rsa.pub -text -noout
    ```
    
    У вихідному файлі має бути виведено модуль:
    
    ```bash
    Public-Key: (2048 bit)
    Modulus:
        00:fe:14:f6:0a:1a:b8:86:19:fe:cd:ab:02:9f:58:
        2f:37:70:15:74:d6:06:9b:81:55:90:99:96:cc:70:
        5c:de:5b:e8:4c:b2:0c:47:5b:a8:a2:98:3d:11:b1:
        f6:7d:a0:46:df:24:23:c6:d0:24:52:67:ba:69:ab:
        9a:4a:6a:66:2c:db:e1:09:f1:0d:b2:b0:e1:47:1f:
        0a:46:ac:0d:82:f3:3c:8d:02:ce:08:43:19:d9:64:
        86:c4:4e:07:12:c0:5b:43:ba:7d:17:8a:a3:f0:3d:
        98:32:b9:75:66:f4:f0:1b:2d:94:5b:7c:1c:e6:f3:
        04:7f:dd:25:b2:82:a6:41:04:b7:50:93:94:c4:7c:
        34:7e:12:7c:bf:33:54:55:47:8c:42:94:40:8e:34:
        5f:54:04:1d:9e:8c:57:48:d4:b0:f8:e4:03:db:3f:
        68:6c:37:fa:62:14:1c:94:d6:de:f2:2b:68:29:17:
        24:6d:f7:b5:b3:18:79:fd:31:5e:7f:4c:be:c0:99:
        13:cc:e2:97:2b:dc:96:9c:9a:d0:a7:c5:77:82:67:
        c9:cb:a9:e7:68:4a:e1:c5:ba:1c:32:0e:79:40:6e:
        ef:08:d7:a3:b9:5d:1a:df:ce:1a:c7:44:91:4c:d4:
        99:c8:88:69:b3:66:2e:b3:06:f1:f4:22:d7:f2:5f:
        ab:6d
    Exponent: 65537 (0x10001)
    ```
    
5. Перенесіть ключ до сховища довірених ключів apk:
    
    ```bash
    sudo mv /tmp/nginx_signing.rsa.pub /etc/apk/keys/
    ```
    
6. Щоб встановити nginx, виконайте команду:
    
    ```bash
    sudo apk add nginx
    ```
    
    Тег `@nginx` також слід вказувати при встановленні пакунків з динамічними модулями:
    
    ```bash
    sudo apk add nginx-module-image-filter@nginx nginx-module-njs@nginx
    ```
    

---

# 📦 Компіляція та встановлення з коду

Компіляція NGINX Open Source з вихідних текстів надає більше гнучкості, ніж готові пакунки: ви можете додавати певні модулі (від NGINX або сторонніх розробників), а також застосовувати найновіші патчі безпеки.

### Встановлення залежностей NGINX

Перед компіляцією NGINX Open Source з вихідних текстів, вам необхідно встановити бібліотеки для його залежностей:

- **PCRE** - Підтримка регулярних виразів. Необхідний для модулів NGINX Core та Rewrite.
    
    ```
    wget github.com/PCRE2Project/pcre2/releases/download/pcre2-10.40/pcre2-10.40.tar.gz
    tar -zxf pcre2-10.40.tar.gz
    cd pcre2-10.40
    ./configure
    make
    sudo make install
    
    ```
    
- **zlib** - Підтримує стиснення заголовків. Вимагається модулем NGINX Gzip.
    
    ```bash
    wget <http://zlib.net/zlib-1.2.13.tar.gz>
    tar -zxf zlib-1.2.13.tar.gz
    cd zlib-1.2.13
    ./configure
    make
    sudo make install
    ```
    
- **OpenSSL** - підтримує протокол HTTPS. Вимагається модулем NGINX SSL та іншими.
    
    ```bash
    wget <http://www.openssl.org/source/openssl-1.1.1p.tar.gz>
    tar -zxf openssl-1.1.1p.tar.gz
    cd openssl-1.1.1p
    ./Configure darwin64-x86_64-cc --prefix=/usr
    make
    sudo make install
    ```
    

### Завантаження вихідних кодів

Завантажте вихідні файли стабільної та основної версій з сайту [nginx.org](http://nginx.org/).

Щоб завантажити і розпакувати вихідні коди для останньої основної версії, виконайте:

```bash
wget [https://nginx.org/download/nginx-1.23.4.tar.gz](https://nginx.org/download/nginx-1.23.2.tar.gz)
tar zxf nginx-1.23.4.tar.gz
cd nginx-1.23.4
```

Щоб завантажити і розпакувати вихідні файли останньої стабільної версії, виконайте:

```bash
wget [https://nginx.org/download/nginx-1.24.0.tar.gz](https://nginx.org/download/nginx-1.24.0.tar.gz)
tar zxf nginx-1.24.0.tar.gz
cd nginx-1.24.0
```

### Налаштування параметрів збірки

Параметри конфігурації визначаються за допомогою скрипта `./configure`, який встановлює різні параметри NGINX, включаючи шляхи до вихідних і конфігураційних файлів, параметри компілятора, методи обробки з'єднань і список модулів. Скрипт завершується створенням Makefile, необхідного для компіляції коду і встановлення NGINX Open Source.

Приклад параметрів скрипта конфігурації (слід вводити в один рядок):

```bash
./configure
--sbin-path=/usr/local/nginx/nginx
--conf-path=/usr/local/nginx/nginx.conf
--pid-path=/usr/local/nginx/nginx.pid
--with-pcre=../pcre2-10.40
--with-zlib=../zlib-1.2.13
--with-http_ssl_module
--with-stream
--with-mail=dynamic
--add-module=/usr/build/nginx-rtmp-module
--add-dynamic-module=/usr/build/3party_module
```

### Налаштування шляхів до NGINX

Скрипт `configure` дозволяє вам встановити шляхи до двійкових і конфігураційних файлів NGINX, а також до залежних бібліотек, таких як PCRE або SSL, щоб статично зв'язати їх з двійковим файлом NGINX.

| Параметр | Опис |
| --- | --- |
| --prefix=<PATH> | Каталог для файлів NGINX, а також базове розташування для всіх відносних шляхів, заданих іншими параметрами конфігураційного скрипта (за винятком шляхів до бібліотек) і для шляху до конфігураційного файлу nginx.conf. Значення за замовчуванням: /usr/local/nginx. |
| --sbin-path=<PATH> | Назва виконуваного файлу NGINX, який використовується лише під час встановлення. Значення за замовчуванням: /sbin/nginx |
| --conf-path=<PATH> | Назва файлу конфігурації NGINX. Однак ви завжди можете замінити це значення під час запуску, вказавши інший файл за допомогою опції -c <FILENAME> у командному рядку nginx. Значення за замовчуванням: conf/nginx.conf |
| --pid-path=<PATH> | Ім'я файлу nginx.pid, в якому зберігається ідентифікатор процесу головного процесу nginx. Після встановлення шлях до цього файлу можна змінити за допомогою директиви pid у конфігураційному файлі NGINX. За замовчуванням: /logs/nginx.pid |
| --error-log-path=<PATH> | Назва основного файлу журналу для помилок, попереджень і діагностичних даних. Після встановлення назву файлу можна змінити за допомогою директиви error_log у файлі конфігурації NGINX. За замовчуванням: /logs/error.log |
| --http-log-path=<PATH> | Ім'я основного лог-файлу для запитів до HTTP-сервера. Після встановлення ім'я файлу завжди можна змінити за допомогою директиви access_log у конфігураційному файлі NGINX. За замовчуванням: /logs/access.log |
| --user=<NAME> | Ім'я непривілейованого користувача, облікові дані якого використовуються робочими процесами NGINX. Після встановлення ім'я можна змінити за допомогою директиви user у файлі конфігурації NGINX. За замовчуванням: nobody |
| --group=<NAME> | Назва групи, облікові дані якої використовуються робочими процесами NGINX. Після встановлення назву можна змінити за допомогою директиви user у конфігураційному файлі NGINX. За замовчуванням: значення, задане директивою --user. |
| --with-pcre=<PATH> | Шлях до коду бібліотеки PCRE, яка потрібна для підтримки регулярних виразів у директиві location та модулі Rewrite. |
| --with-pcre-jit |  Збирає бібліотеку PCRE з підтримкою компіляції "just-in-time" (директива pcre_jit). |
| --with-zlib=<PATH> | Шлях до коду бібліотеки zlib, яку вимагає модуль Gzip. |

**Налаштування параметрів NGINX GCC**

За допомогою скрипта `configure` ви також можете вказати параметри, пов'язані з компілятором.

| Параметр | Опис |
| --- | --- |
| --with-cc-opt="<PARAMETERS>" | Додаткові параметри, які додаються до змінної CFLAGS. При використанні системної бібліотеки PCRE у FreeBSD обов'язковим значенням є --with-cc-opt="-I /usr/local/include". Якщо потрібно збільшити кількість файлів, що підтримуються select(), це також можна вказати тут, як у цьому прикладі: --with-cc-opt="-D FD_SETSIZE=2048". |
| --with-ld-opt="<PARAMETERS>" | Додаткові параметри, які використовуються під час лінкування. При використанні системної бібліотеки PCRE під FreeBSD обов'язковим значенням є --with-ld-opt="-L /usr/local/lib". |

**Вказівка методів обробки з'єднань NGINX**

За допомогою скрипта `configure` ви можете перевизначити метод опитування на основі подій.

| Назва модуля | Опис |
| --- | --- |
| --with-select_module, --without-select_module | Дозволяє або забороняє створення модуля, який дозволяє NGINX працювати з методом select(). Модуль буде створено автоматично, якщо платформа не підтримує відповідні методи, такі як kqueue, epoll або /dev/poll. |
| --with-poll_module, --without-poll_module | Дозволяє або забороняє збірку модуля, який дозволяє NGINX працювати з методом poll(). Модуль буде зібрано автоматично, якщо виявиться, що платформа не підтримує відповідні методи, такі як kqueue, epoll або /dev/poll. |

**Вибір модулів NGINX для збірки**

NGINX складається з набору функціонально-специфічних модулів, які вказуються за допомогою скрипта `configure` разом з іншими параметрами збірки.

Деякі модулі збираються за замовчуванням - їх не потрібно вказувати у скрипті `configure`. Однак, модулі за замовчуванням можна явно виключити з двійкового файла NGINX за допомогою опції `--without-<MODULE-NAME>` у скрипті `configure`.

Модулі, не включені за замовчуванням, а також модулі сторонніх розробників мають бути явно вказані у скрипті `configure` разом з іншими параметрами збірки. Такі модулі можуть бути зв'язані з бінарним файлом NGINX статично (тоді вони завантажуються при кожному запуску NGINX) або динамічно (вони завантажуються тільки якщо відповідні директиви включено до конфігураційного файлу NGINX).

**Модулі, що збираються за замовчуванням**

Якщо вам не потрібен модуль, який збирається за замовчуванням, ви можете вимкнути його, назвавши його за допомогою опції `--without-<MODULE-NAME>` у скрипті конфігурації, як у цьому прикладі, який вимикає модуль Empty GIF (слід вводити у вигляді одного рядка):

```bash
./configure
--sbin-path=/usr/local/nginx/nginx
--conf-path=/usr/local/nginx/nginx.conf
--pid-path=/usr/local/nginx/nginx.pid
--with-http_ssl_module
--with-stream
--with-pcre=../pcre2-10.40
--with-zlib=../zlib-1.2.13
--without-http_empty_gif_module
```

| Назва модуля | Опис |
| --- | --- |
| http_access_module | Приймає або відхиляє запити з вказаних клієнтських адрес. |
| http_auth_basic_module | Обмежує доступ до ресурсів шляхом перевірки імені користувача та пароля за допомогою протоколу HTTP Basic Authentication. |
| http_autoindex_module | Обробляє запити, що закінчуються символом прямої косої риски (/), і видає список каталогів. |
| http_browser_module | Створює змінні, значення яких залежать від значення заголовка запиту User-Agent. |
| http_charset_module | Додає вказаний набір символів до заголовка Content-Type відповіді. Може конвертувати дані з одного набору символів в інший. |
| http_empty_gif_module | Видає однопіксельний прозорий GIF. |
| http_fastcgi_module | Передає запити до сервера FastCGI. |
| http_geo_module | Створює змінні зі значеннями, що залежать від IP-адреси клієнта. |
| http_gzip_module | Стискає відповіді за допомогою gzip, зменшуючи обсяг переданих даних вдвічі або більше. |
| http_limit_conn_module | Обмежує кількість з'єднань за певним ключем, зокрема, кількість з'єднань з однієї IP-адреси. |
| http_limit_req_module | Обмежує швидкість обробки запитів за певним ключем, зокрема, швидкість обробки запитів, що надходять з однієї IP-адреси. |
| http_map_module | Створює змінні, значення яких залежать від значень інших змінних. |
| http_memcached_module | Передає запити на кешований сервер. |
| http_proxy_module | Передає HTTP-запити на інший сервер. |
| http_referer_module | Блокує запити з невірними значеннями в заголовку Referer. |
| http_rewrite_module | Змінює URI запиту за допомогою регулярних виразів і повертає перенаправлення; умовно вибирає конфігурації. Потрібна бібліотека PCRE. |
| http_scgi_module | Передає запити на SCGI-сервер. |
| http_ssi_module | Обробляє команди SSI (Server Side Includes) у відповідях, що проходять через нього. |
| http_split_clients_module | Створює змінні, придатні для A/B (спліт) тестування. |
| http_upstream_hash_module | Вмикає загальний метод балансування навантаження Hash. |
| http_upstream_ip_hash_module | Вмикає метод балансування навантаження IP Hash. |
| http_upstream_keepalive_module | Вмикає постійні з'єднання. |
| http_upstream_least_conn_module | Вмикає метод балансування навантаження Least Connections. |
| http_upstream_zone_module | Вмикає зони спільної пам'яті. |
| http_userid_module | Встановлює файли cookie для ідентифікації клієнта. |
| http_uwsgi_module | Передає запити на сервер uwsgi. |

**Включення модулів, які не збираються за замовчуванням**

Багато модулів NGINX не збираються за замовчуванням, і їх потрібно вказати у командному рядку configure, щоб їх було зібрано.

Модулі `mail`, `stream`, `geoip`, `image_filter`, `perl` і `xslt` можна скомпілювати як динамічні. Докладні відомості наведено трохи нижче.

Приклад команди `configure`, яка включає нетипові модулі (слід вводити у вигляді одного рядка):

```bash
./configure
--sbin-path=/usr/local/nginx/nginx
--conf-path=/usr/local/nginx/nginx.conf
--pid-path=/usr/local/nginx/nginx.pid
--with-pcre=../pcre2-10.40
--with-zlib=../zlib-1.2.13
--with-http_ssl_module
--with-stream
--with-mail
```

| Назва модуля | Опис |
| --- | --- |
| —with-cpp_test_module | Тестує C++ сумісність заголовних файлів. |
| --with-debug | Вмикає журнал налагодження. |
| --with-file-aio | Вмикає асинхронний ввід/вивід. Дозволяє використовувати бібліотеку інструментів Google Performance. |
| --with-http_addition_module | Додає текст до і після відповіді. |
| --with-http_auth_request_module | Реалізує авторизацію клієнта на основі результату підзапиту. |
| --with-http_dav_module | Вмикає автоматизацію керування файлами за допомогою протоколу WebDAV. |
| --with-http_degradation_module | Дозволяє повертати помилку, коли розмір пам'яті перевищує визначене значення. |
| --with-http_flv_module | Забезпечує псевдопотокову серверну підтримку файлів Flash Video (FLV).
 |
| --with-http_geoip_module | Дозволяє створювати змінні, значення яких залежать від IP-адреси клієнта. Модуль використовує бази даних MaxMind GeoIP. Для компіляції як окремого динамічного модуля змініть параметр на --with-http_geoip_module=dynamic. |
| —with-http_gunzip_module | Розпаковує відповіді з Content-Encoding: gzip для клієнтів, які не підтримують метод кодування zip. |
| --with-http_gzip_static_module | Дозволяє надсилати попередньо стиснуті файли з розширенням .gz замість звичайних файлів. |
| --with-http_image_filter_module | Перетворює зображення у форматах JPEG, GIF та PNG. Для роботи модуля потрібна бібліотека LibGD. Для компіляції як окремого динамічного модуля змініть параметр на --with-http_image_filter_module=dynamic. |
| --with-http_mp4_module | Надає псевдопотокову серверну підтримку для файлів MP4. |
| --with-http_perl_module | Використовується для реалізації обробників місцезнаходження і змінних на Perl і вставки викликів Perl в SSI. Вимагає наявності бібліотеки PERL. Для компіляції як окремого динамічного модуля змініть параметр на --with-http_perl_module=dynamic. |
| --with-http_random_index_module | Обробляє запити, що закінчуються символом косої риски ('/'), і вибирає випадковий файл у каталозі як індексний файл. |
| --with-http_realip_module | Змінює адресу клієнта на ту, яку було надіслано у вказаному полі заголовка. |
| --with-http_secure_link_module | Використовується для перевірки автентичності запитуваних посилань, захисту ресурсів від несанкціонованого доступу та обмеження часу життя посилання. |
| --with-http_slice_module | Дозволяє розбивати запит на підзапити, кожен з яких повертає певний діапазон відповідей. Забезпечує більш ефективне кешування великих файлів. |
| --with-http_ssl_module | Вмикає підтримку HTTPS. Потрібна бібліотека SSL, наприклад, OpenSSL. |
| --with-http_stub_status_module | Надає доступ до базової інформації про стан |
| --with-http_sub_module | Змінює відповідь, замінюючи один вказаний рядок іншим. |
| --with-http_xslt_module | Перетворює XML-відповіді за допомогою однієї або декількох таблиць стилів XSLT. Для роботи модуля потрібні бібліотеки Libxml2 і XSLT. Щоб скомпілювати його як окремий динамічний модуль, змініть параметр на --with-http_xslt_module=dynamic. |
| --with-http_v2_module | Вмикає підтримку HTTP/2. |
| --with-mail | Вмикає функціональність поштового проксі. Для компіляції як окремого динамічного модуля змініть цей параметр на --with-mail=dynamic. |
| --with-mail_ssl_module | Надає підтримку поштовому проксі-серверу для роботи з протоколом SSL/TLS. Потрібна бібліотека SSL (напр. OpenSSL). |
| --with-stream | Вмикає функціональність TCP і UDP проксі. Для компіляції як окремого динамічного модуля змініть параметр на --with-stream=dynamic. |
| --with-stream_ssl_module | Надає підтримку потокового проксі-сервера для роботи з протоколом SSL/TLS. Потрібна бібліотека SSL, наприклад OpenSSL. |
| --with-threads | - Дозволяє NGINX використовувати пули потоків. Докладнішу інформацію можна знайти https://www.nginx.com/blog/thread-pools-boost-performance-9x/?_ga=2.7036080.361969350.1682701225-1458802245.1682411140. |

**Динамічно зв'язані модулі**

Модулі NGINX також можуть бути скомпільовані як спільний об'єкт (`.so`-файл), а потім динамічно завантажені у NGINX Open Source під час виконання. Це забезпечує більшу гнучкість, оскільки модуль можна завантажити або вивантажити у будь-який час, додавши або видаливши відповідну директиву `load_module` у конфігураційному файлі NGINX і перезавантаживши конфігурацію. Зауважте, що сам модуль має підтримувати динамічне компонування.

Щоб скомпілювати NGINX Open Source з динамічно завантаженим модулем стороннього виробника, додайте опцію `--add-dynamic-module=<PATH>` до команди `configure`, де `<PATH>` - це шлях до вихідного коду:

```bash
./configure ... --add-dynamic-module=<PATH>
```

Отримані `.so` файли записуються в каталог `prefix/modules/`, де префікс - це каталог для серверних файлів, наприклад, `/usr/local/nginx/`.

Щоб завантажити динамічний модуль, додайте директиву `load_module` до конфігурації NGINX після встановлення:

```bash
load_module modules/ngx_mail_module.so;
```

### Завершення встановлення з коду

Складіть і встановіть збірку:

```bash
make
sudo make install
```

---

Після завершення встановлення NGINX має бути автоматично зареєстрований і запущений як служба `systemd`. Для перевірки виконайте наступну команду:

```bash
sudo systemctl status nginx

# ● nginx.service - A high performance web server and a reverse proxy server
#      Loaded: loaded (/lib/systemd/system/nginx.service; enabled; vendor preset: enabled)
#      Active: active (running)
```

Якщо в статусі вказано, що служба не запущена `Active: inactive (dead)`, то ви можете запустити службу, виконавши цю команду:

```bash
sudo systemctl start nginx
```

Для візуальної перевірки того, що все працює належним чином, відвідайте ваш сервер/віртуальну машину за допомогою вашого браузера, і ви побачите стандартну сторінку привітання NGINX:

![Untitled](%D0%A3%D1%81%D1%82%D0%B0%D0%BD%D0%BE%D0%B2%D0%BA%D0%B0%20%D1%82%D0%B0%20%D0%BD%D0%B0%D0%BB%D0%B0%D1%88%D1%82%D1%83%D0%B2%D0%B0%D0%BD%D0%BD%D1%8F%207e1859516fe9420c9f09c2e9618af347/Untitled.png)

---

Джерела:

- [https://docs.nginx.com/nginx/admin-guide/installing-nginx/installing-nginx-open-source/](https://docs.nginx.com/nginx/admin-guide/installing-nginx/installing-nginx-open-source/)
- [https://www.freecodecamp.org/news/the-nginx-handbook/](https://www.freecodecamp.org/news/the-nginx-handbook/)